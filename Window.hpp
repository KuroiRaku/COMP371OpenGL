#ifndef WINDOW_H
#define WINDOW_H

struct Window {
public:

     void create(unsigned int width, unsignd int height);
     void update();
     void clear();
     void destroy();

private:

};


#endif
